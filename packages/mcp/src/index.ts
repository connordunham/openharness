/**
 * @openharness/mcp — local MCP server over the core API (spec §8.7),
 * mirroring the reference tool's own hosted MCP endpoint but pointed at a local
 * .ohd file. Tools: get_document, list_components, list_wires, get_nets,
 * get_bom, get_diagnostics, add_*, update_*, delete, assign_part,
 * search_parts, set_bundle_length, export.
 *
 * SCAFFOLDING — Phase 5 per the build plan (spec §12). Not implemented yet.
 */
export {};
