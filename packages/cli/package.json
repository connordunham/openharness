{
  "name": "openharness",
  "version": "0.0.1",
  "description": "Headless CLI: validate, export, run automations, query, import, diff, doctor (spec §8.6).",
  "type": "module",
  "main": "./dist/index.js",
  "bin": { "openharness": "./dist/cli.js" },
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run"
  },
  "dependencies": {
    "@openharness/core": "*",
    "@openharness/io": "*",
    "@openharness/automation": "*"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
