{
  "name": "@openharness/core",
  "version": "0.0.1",
  "description": "Document model, transactions, derived model, net/route/BOM/DRC algorithms. No DOM dependency.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run"
  },
  "dependencies": {
    "immer": "^10.0.0",
    "nanoid": "^5.0.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
