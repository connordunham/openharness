/**
 * @openharness/automation — the plugin host (spec §8): manifest loader,
 * Worker/worker_threads sandbox, scoped permissions (write:wires,
 * write:components, write:parts, write:meta), command/rule/panel/exporter
 * registries, and the AutomationContext API surface used by scripts in
 * automations/.
 *
 * SCAFFOLDING — Phase 5 per the build plan (spec §12), moved up from last
 * place per review R17 since this is the actual point of the project.
 * Not implemented yet.
 */
export {};
